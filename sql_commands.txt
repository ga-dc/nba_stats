  -All columns for all players from the New York Knicks (NYK)
  1.SELECT * FROM players WHERE team = 'NYK';
  -  All columns for all players from the Indiana Packers (IND) who are under 26 years old
  2.SELECT * FROM players WHERE team = 'IND'AND age < 26;
  -All columns for all players, ordered from least points scored to most points scored
  3.SELECT * FROM players ORDER BY points ASC;
  -Name and Points per game (points/games), for the players with the top 20 points per game
  4.SELECT name,(points/games) AS pointspergames FROM players  ORDER BY pointspergames DESC LIMIT 20;
  -The average age for all players
  5.SELECT AVG(age) FROM players;
  -The average age for all players on the Oklahoma City Thunder (OKC)
  6.SELECT AVG(age) FROM players WHERE team = 'OKC';
  -The average age for all players who played more than 40 games
  7.SELECT AVG(age) FROM players WHERE games > 40;
  -The team and total points scored from all players on that team (team points), ordered from most team points to least
  8.SELECT team,SUM(points) AS totalpoints FROM players GROUP BY team ORDER BY totalpoints DESC;
